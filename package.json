{
  "name": "nijaru-github-io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "generate:resume": "bun scripts/generate-resume.ts",
    "build:resume": "bun run generate:resume && cd resume && xelatex -interaction=nonstopmode resume.tex && cp resume.pdf ../public/Nicholas_Russo_Resume.pdf && rm -f resume.aux resume.log resume.out",
    "preview": "astro preview",
    "astro": "astro",
    "check-deps": "bun scripts/check-deps.js"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.14",
    "@astrojs/sitemap": "^3.6.0",
    "@fontsource/atkinson-hyperlegible": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "astro": "^5.16.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.17",
    "tailwindcss": "^4.1.17",
    "terser": "^5.44.1"
  }
}
