---
import Layout from "../layouts/Layout.astro";
import ReflectionText from "../components/ReflectionText";
import TextGlow from "../components/TextGlow";
import BlogPostPreview from "../components/BlogPostPreview";
import GithubRepos from "../components/GithubRepos";
import ArrowLink from "../components/ArrowLink";

// Get all blog posts and sort by date (newest first)
// This will make the home page always show the latest post
const allPosts = [
    {
        url: "/blog/welcome-to-my-new-site",
        title: "Rebuilding My Personal Site with Claude Code, Solid.js, and Astro",
        pubDate: new Date("2024-03-01"),
        formattedDate: "March 1, 2024",
        excerpt:
            "My experience using Claude Code to rebuild my personal website with Solid.js and Astro featuring a space theme.",
        tags: ["Claude Code", "AI", "Solid.js", "Astro", "Web Dev"],
    },
    // When you add more posts, they'll automatically be included and sorted here
];

// Sort posts by date (newest first)
allPosts.sort((a, b) => new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime());

// Get the most recent post (or undefined if no posts)
const mostRecentPost = allPosts.length > 0 ? allPosts[0] : undefined;
---

<Layout title="Nick Russo - Software Developer">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center mb-12">
            <h1 class="text-5xl md:text-6xl font-bold mb-4">
                <span class="text-accent-purple">
                    <TextGlow client:load enhanced={true}>
                        <ReflectionText client:load>Nick</ReflectionText>
                    </TextGlow>
                </span>
                <span class="text-white">
                    <TextGlow client:load enhanced={true}>
                        <ReflectionText client:load>Russo</ReflectionText>
                    </TextGlow>
                </span>
            </h1>

            <h2 class="text-2xl md:text-3xl font-semibold mb-8 tracking-wide">
                <TextGlow client:load>
                    <span class="text-lime-400">Software Developer</span>
                </TextGlow>
            </h2>
        </div>

        <!-- About Me Section -->
        <section class="mb-12">
            <h2 class="text-2xl font-bold mb-6 border-b border-space-700 pb-2">
                <span class="text-lime-400">About Me</span>
            </h2>
            <div class="card-container mb-4">
                <p class="text-gray-300 mb-4">
                    I'm a software developer specializing in backend systems and
                    distributed applications. With experience in Python, Go, and various
                    cloud technologies, I build reliable and efficient solutions for
                    complex problems.
                </p>
                <p class="text-gray-300">
                    Currently exploring AI applications, database innovations, and
                    modern frontend frameworks like Solid.js and Astro. Starting a new
                    pre seed post failed startup every day in <span
                        class="text-lime-300 font-medium">Flounder mode™.</span
                    >
                </p>
            </div>
            <div class="mt-4 text-right">
                <ArrowLink href="/about" client:load ariaLabel="Navigate to about page">
                    Learn more about me
                </ArrowLink>
            </div>
        </section>

        <!-- Recent Blog Post -->
        <section class="mb-12">
            <h2 class="text-2xl font-bold mb-6 border-b border-space-700 pb-2">
                <span class="text-lime-400">Recent Post</span>
            </h2>

            {
                mostRecentPost ? (
                    <BlogPostPreview post={mostRecentPost} />
                ) : (
                    <div class="card-container text-center py-6">
                        <p class="text-gray-300">
                            No blog posts available yet. Check back soon!
                        </p>
                    </div>
                )
            }

            <div class="mt-4 text-right">
                <ArrowLink href="/blog" client:load ariaLabel="Navigate to blog page">
                    View all posts
                </ArrowLink>
            </div>
        </section>

        <!-- Featured GitHub Repos -->
        <section>
            <h2 class="text-2xl font-bold mb-6 border-b border-space-700 pb-2">
                <span class="text-lime-400">Pinned Repositories</span>
            </h2>
            <GithubRepos client:load username="nijaru" limit={2} />
            <div class="mt-4 text-right">
                <ArrowLink
                    href="/projects"
                    client:load
                    ariaLabel="Navigate to projects page"
                >
                    View all projects
                </ArrowLink>
            </div>
        </section>
    </div>
</Layout>
