---
// TextGlow component - Simple text with accent color
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={["text-accent-primary", className]}>
  <slot />
</span>