---
// ArrowLink component - Link with arrow
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class:list={["link-primary link-arrow", className]}>
  <slot />
</a>