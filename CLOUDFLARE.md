# Cloudflare Pages

Cloudflare Pages auto-detects bun from bun.lock.

Build configuration:
- Build command: `bun run build`
- Output directory: `dist`
- Node version: 20 (from .node-version)

Note: Resume PDF is pre-generated and committed. 
Run `bun run build:resume` locally when updating resume.tex.
